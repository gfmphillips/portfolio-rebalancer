[project]
name = "portfolio-rebalancer"
version = "0.1.0"
description = "CLI tool to rebalance Fidelity investment accounts"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "typer>=0.9",
    "rich>=13.0",
    "pyyaml>=6.0",
    "streamlit>=1.30",
    "plotly>=5.18",
    "requests>=2.31",
]

[project.scripts]
rebalancer = "rebalancer.cli:app"
rebalancer-web = "rebalancer.run_web:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/rebalancer"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[dependency-groups]
dev = [
    "pytest>=7.0",
]
